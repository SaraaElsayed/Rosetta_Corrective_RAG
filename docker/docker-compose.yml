services:
  fastapi:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: fastapi
    ports:
      - "8000:8000"
    restart: always
    env_file:
      - ./.env.app   # relative to this file (docker/) — only place you need it